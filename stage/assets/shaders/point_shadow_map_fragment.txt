#version 330 core

uniform vec3 lightPos;
uniform float farPlane;

in vec3 fragPos;

out vec4 fragColor;

void main() {
    float distance = length(fragPos - lightPos);
    float normalizedDepth = distance / farPlane;
    fragColor = vec4(vec3(normalizedDepth), 1.0);
}
